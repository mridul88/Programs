#include<stdio.h>
#include<conio.h>
int main()
{
	int *a,n,m,**arr;
	scanf("%d %d",&n,&m);
	a=new (int [sizeof n]);
	arr=new(int *a);
	for(int i=0;i<n;i++)
	{
		for(int j=0;j<m;j++)
		{	arr[i]+a[j]=1;

			printf("%d\t",arr[i]+a[j]);
		}
		printf("\n");

	}
	getch();
	return 0;
}