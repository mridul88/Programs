#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{  clrscr();
	long int n,p=2;
	scanf("%ld",&n);
	printf("\nprime factors are:- \n");
	while(p<sqrt(n))
	{
		int flag=0;
		for(int i=2;i<p;i++)
		{
			int r=p%i;
			if(r==0)
			{	flag=1;
				break;
			}
		}
		if(flag==0)
		{
			int r=n%p;
			if(r==0)
				printf("%ld\n",p);
		}
		p++;
	}
	getch();
	return 0;
}

