#include<stdio.h>
#include<string.h>

int main2( int argc,char * argv[])
{
    char *grade=" ";
    //scanf("%d", &n);
    //grade[1]="mridul";
    //grade=(char *)malloc(n*sizeof(int));
    //for(i=0;i<n;i++)
    //{
     scanf("%s",grade);
     printf("sdfghjk");
     printf("%s",grade);
     //}
    compare(grade);
    getch();
    return 0;
}
int compare(char* grade)
{    
     if(strcmp(grade,"a+")==0)
    {
                  printf("fghjkgfd");
    }
    else
    {
        printf("working");
    }
    getch();
    return 0;
}
